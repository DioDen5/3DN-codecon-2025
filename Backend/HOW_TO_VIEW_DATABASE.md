# Як переглянути локальну базу даних MongoDB

## 📊 Інформація про вашу базу даних

- **Connection String**: `mongodb://127.0.0.1:27017/studlink`
- **Host**: `127.0.0.1` (localhost)
- **Port**: `27017` (стандартний порт MongoDB)
- **Database Name**: `studlink`

## 🔧 Варіанти перегляду бази даних

### 1. MongoDB Compass (Рекомендовано) ⭐

**MongoDB Compass** - це офіційний GUI інструмент від MongoDB для роботи з базою даних.

#### Встановлення:
1. Завантажте MongoDB Compass: https://www.mongodb.com/try/download/compass
2. Встановіть на ваш комп'ютер

#### Підключення:
1. Відкрийте MongoDB Compass
2. В полі "Connection String" введіть:
   ```
   mongodb://127.0.0.1:27017
   ```
3. Натисніть "Connect"
4. В лівому меню виберіть базу даних `studlink`
5. Тепер ви можете переглядати всі колекції та документи!

#### Що ви побачите:
- Всі колекції (users, teachers, announcements, тощо)
- Документи в кожній колекції
- Можливість редагувати, видаляти, додавати документи
- Можливість виконувати запити

### 2. Через термінал (mongosh)

Якщо у вас встановлений MongoDB, ви можете використовувати `mongosh`:

```bash
mongosh mongodb://127.0.0.1:27017/studlink
```

Потім виконайте команди:
```javascript
// Показати всі колекції
show collections

// Знайти користувача
db.users.findOne({ email: /denys.*zastavnyi/i })

// Показати всіх користувачів
db.users.find().pretty()

// Знайти токени скидання пароля
db.passwordresettokens.find().sort({ createdAt: -1 }).limit(5)
```

### 3. VS Code Extension (MongoDB for VS Code)

Якщо ви використовуєте VS Code:
1. Встановіть розширення "MongoDB for VS Code"
2. Підключіться до `mongodb://127.0.0.1:27017`
3. Виберіть базу `studlink`

## ❌ Чи можна побачити на сайті mongodb.com?

**НІ!** Сайт mongodb.com показує тільки **MongoDB Atlas** (хмарну базу даних), а не вашу локальну базу.

- **Локальна база** = працює на вашому комп'ютері (`127.0.0.1`)
- **MongoDB Atlas** = хмарна база на серверах MongoDB

Це дві різні речі! Ваша локальна база доступна тільки на вашому комп'ютері.

## 🔍 Швидкий пошук користувача в Compass

1. Відкрийте колекцію `users`
2. Натисніть "Filter" (фільтр)
3. Введіть:
   ```json
   { "email": { "$regex": "denys.*zastavnyi", "$options": "i" } }
   ```
4. Натисніть "Find"

Або просто введіть в пошук:
```
Denys.Zastavnyi@lnu.edu.ua
```

## 📝 Перевірка змін після скидання пароля

1. **ДО скидання пароля:**
   - Відкрийте документ користувача в Compass
   - Запишіть значення `passwordHash` (або зробіть скріншот)
   - Запишіть `updatedAt`

2. **Скиньте пароль через форму на сайті**

3. **ПІСЛЯ скидання пароля:**
   - Оновіть документ в Compass (натисніть "Refresh")
   - Порівняйте:
     - `passwordHash` має змінитися (новий хеш)
     - `updatedAt` має оновитися
     - В колекції `passwordresettokens` токен має мати `used: true`

## 💡 Поради

- **MongoDB Compass** - найзручніший спосіб для візуального перегляду
- Ви можете редагувати документи прямо в Compass (натисніть на документ → "Edit Document")
- Використовуйте фільтри для швидкого пошуку
- Зберігайте скріншоти для порівняння "до" і "після"

