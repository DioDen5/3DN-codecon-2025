# 📊 Інструкція: Імпорт схеми бази даних в Moon Modeler

## Крок 1: Очищення бази даних

Спочатку видалимо невикористовувані колекції:

```bash
cd Backend
npm run cleanup-db
```

Це безпечно видалить:
- ✅ Колекцію `reviews` (модель Review не використовується)
- ✅ Колекцію `student_verifications` (модель StudentVerification не використовується)

**⚠️ Важливо:** Скрипт видалить тільки ці дві колекції. Всі інші дані залишаться недоторканими.

---

## Крок 2: Встановлення Moon Modeler для MongoDB

⚠️ **ВАЖЛИВО:** Потрібна саме версія **"Moon Modeler for MongoDB"**, а не звичайна версія для реляційних БД!

### Варіант A: Moon Modeler for MongoDB (платна)

1. Завантажте з офіційного сайту: https://www.datensen.com/data-modeling/moon-modeler-for-mongodb.html
2. Встановіть програму (є версії для Windows, macOS, Linux)
3. Можна використати trial версію для тестування

### Варіант B: Mongo Modeler (безкоштовна онлайн альтернатива)

1. Відкрийте в браузері: https://mongomodeler.com/
2. Не потребує встановлення - працює онлайн
3. Підтримує підключення до MongoDB та візуалізацію колекцій

---

## Крок 3: Підключення до MongoDB

### Варіант A: Moon Modeler for MongoDB (локальна програма)

#### Якщо MongoDB запущена локально:

1. **Відкрийте Moon Modeler for MongoDB** (не звичайну версію!)
2. **File → New Project → MongoDB** (тепер MongoDB буде в списку опцій!)
3. **Налаштуйте підключення:**
   - **Connection Name:** `StudLink Local`
   - **Host:** `127.0.0.1` або `localhost`
   - **Port:** `27017` (стандартний порт MongoDB)
   - **Database:** `studlink` (або назва вашої БД з `.env`)
   - **Authentication:** 
     - Якщо MongoDB без пароля: залиште порожнім
     - Якщо є пароль: введіть username/password
4. **Натисніть "Test Connection"** - має з'явитися ✅
5. **Натисніть "OK"**

#### Якщо MongoDB на віддаленому сервері:

1. **Відкрийте Moon Modeler for MongoDB**
2. **File → New Project → MongoDB**
3. **Налаштуйте підключення:**
   - **Connection Name:** `StudLink Remote`
   - **Host:** IP-адреса або домен вашого сервера
   - **Port:** `27017` (або інший, якщо змінений)
   - **Database:** `studlink`
   - **Authentication:** введіть username/password
4. **Натисніть "Test Connection"**
5. **Натисніть "OK"**

### Варіант B: Mongo Modeler (онлайн, безкоштовно)

1. **Відкрийте:** https://mongomodeler.com/
2. **Натисніть "Connect to MongoDB"**
3. **Введіть дані підключення:**
   - **Host:** `127.0.0.1` (або IP сервера)
   - **Port:** `27017`
   - **Database:** `studlink`
   - **Username/Password:** (якщо потрібно)
4. **Натисніть "Connect"**
5. **Автоматично завантажаться всі колекції!** 🎉

---

## Крок 4: Автоматичний імпорт схеми (Reverse Engineering)

### Moon Modeler for MongoDB:

1. **Після підключення** Moon Modeler автоматично покаже всі колекції
2. **Виберіть колекції для імпорту:**
   - Можна вибрати всі колекції (рекомендовано)
   - Або вибрати конкретні колекції
3. **Натисніть "Import"** або "Reverse Engineer"
4. **Moon Modeler автоматично:**
   - ✅ Проаналізує структуру кожної колекції
   - ✅ Визначить типи полів
   - ✅ Створить діаграму зі зв'язками між колекціями
   - ✅ Покаже індекси та обмеження

### Mongo Modeler (онлайн):
1. Після підключення всі колекції вже відображаються в списку
2. **Клікніть на колекцію** → побачите структуру полів
3. **Можна переглядати документи** та їх структуру
4. **Експортувати** можна як JSON або зробити скріншот

---

## Крок 5: Налаштування діаграми

### Moon Modeler for MongoDB:

Після імпорту ви побачите діаграму з усіма колекціями. Можете:

1. **Переміщувати блоки** мишею для кращого розташування
2. **Змінювати кольори** колекцій (клік правою → Properties → Color)
3. **Додавати коментарі** до колекцій/полів
4. **Експортувати діаграму:**
   - **File → Export → Image** (PNG, SVG, PDF)
   - **File → Export → SQL** (якщо потрібно для документації)

---

## Крок 6: Збереження проекту

1. **File → Save Project**
2. Збережіть файл як `studlink-database-model.mdbm` (або інша назва)
3. Тепер ви можете:
   - Відкривати проект пізніше
   - Експортувати діаграму для курсової роботи
   - Синхронізувати зміни з БД

---

## 📝 Список колекцій, які будуть імпортовані:

Після очищення в БД залишаться такі колекції:

✅ **users** - користувачі системи
✅ **teachers** - профілі викладачів
✅ **teachercomments** - відгуки про викладачів
✅ **teacherclaimrequests** - заявки на прив'язку профілю
✅ **announcements** - обговорення на форумі
✅ **comments** - коментарі до обговорень
✅ **reactions** - реакції (лайки/дизлайки)
✅ **reports** - скарги на контент
✅ **namechangerequests** - заявки на зміну імені
✅ **emailverificationcodes** - коди верифікації email
✅ **emailverifytokens** - токени верифікації
✅ **passwordresettokens** - токени скидання пароля
✅ **refreshtokens** - токени оновлення сесії
✅ **userprofiles** - додаткові профілі користувачів
✅ **activitylogs** - логи активності
✅ **loginattempts** - спроби входу
✅ **securitysettings** - налаштування безпеки

---

## 🔄 Синхронізація змін

Якщо ви змінили структуру БД в коді:

1. **Tools → Synchronize with Database**
2. Moon Modeler оновить діаграму відповідно до поточної структури БД

---

## 💡 Поради

- **Регулярно синхронізуйте** проект з БД, якщо вносите зміни
- **Експортуйте діаграму** в PDF для курсової роботи
- **Додавайте коментарі** до полів для кращого розуміння структури
- **Групуйте колекції** за функціональністю (користувачі, контент, безпека)

---

## ❓ Проблеми?

### Не бачу опції MongoDB в Moon Modeler
**Рішення:** Ви встановили звичайну версію Moon Modeler (для реляційних БД). Потрібна саме **"Moon Modeler for MongoDB"**:
- Завантажте: https://www.datensen.com/data-modeling/moon-modeler-for-mongodb.html
- АБО використайте безкоштовну альтернативу: https://mongomodeler.com/

### Помилка підключення
- Перевірте, чи запущена MongoDB: `mongosh` або `mongo`
- Перевірте правильність host/port у `.env`
- Перевірте firewall налаштування

### Колекції не відображаються
- Перевірте назву бази даних в `.env`
- Перевірте права доступу користувача MongoDB

### Діаграма виглядає заплутано
- Використовуйте автоматичне розташування: **Layout → Auto Layout**
- Групуйте колекції вручну
- Приховуйте непотрібні поля: клік правою → Hide Field

---

**Готово!** Тепер у вас є візуальна схема бази даних для курсової роботи! 🎉

